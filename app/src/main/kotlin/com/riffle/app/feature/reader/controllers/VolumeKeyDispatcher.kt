package com.riffle.app.feature.reader.controllers

// Moved to :feature:reader/commonMain for iOS parity; alias keeps app imports unchanged.
typealias VolumeKeyDispatcher = com.riffle.feature.reader.VolumeKeyDispatcher
