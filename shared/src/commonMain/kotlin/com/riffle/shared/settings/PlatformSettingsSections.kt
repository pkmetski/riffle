package com.riffle.shared.settings

import androidx.compose.runtime.Composable

/** Platform-specific Settings sections. Currently empty on iOS. */
@Composable
fun PlatformSettingsSections() {
    // Android-only rows (volume keys, APK update, SAF) are in the Android app/ module.
}
